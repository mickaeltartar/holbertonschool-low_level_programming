#include "main.h"

/**
 * _abs - Calcul la valeur absolu de int.
 *
 * @n: nombre.
 *
 * Return: la valeur absolue de int.
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

